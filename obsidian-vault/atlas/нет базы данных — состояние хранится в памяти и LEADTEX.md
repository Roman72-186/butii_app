---
tags: [atlas, storage, architecture]
date: 2026-04-02
---

# Нет базы данных — состояние хранится в памяти и LEADTEX

## Где что хранится

| Данные | Где хранятся | Как долго |
|---|---|---|
| Корзина (объекты товаров) | `cart[]` в памяти JS | До перезагрузки страницы |
| `telegram_id` пользователя | `localStorage` | Постоянно (до очистки браузера) |
| Данные контакта | LEADTEX CRM | Постоянно |
| История заказов | LEADTEX CRM | Постоянно |
| Каталог товаров | `js/config.js` (статика) | Деплой |

## Следствия этой архитектуры

**Плюсы:**
- Нет своего сервера, нет своей БД — нет расходов и поддержки
- Деплой = пуш в GitHub

**Минусы:**
- Корзина сбрасывается при закрытии вкладки (нет сохранения в `localStorage`)
- Секция «Мои заказы» — заглушка, реальной истории нет без API к LEADTEX
- Нельзя показать статус заказа внутри Mini App без обратного вызова от LEADTEX

## telegram_id как идентификатор пользователя

Приложение не имеет собственной системы пользователей. Вместо этого:

1. Telegram передаёт `id` через `window.Telegram.WebApp.initDataUnsafe.user`
2. `telegram.js` сохраняет его в `localStorage` при инициализации
3. `app.js::sendOrderToServer()` читает из `localStorage` с fallback на `window.Telegram.WebApp`

Это означает: **пользователь должен сначала написать `/start` боту**, чтобы LEADTEX создал для него контакт. Если контакта нет — поиск по `telegram_id` вернёт пустой результат.

Подробнее: [[LEADTEX принимает заказы через inner_webhook]]

## Связанные заметки

- [[стек технологий — vanilla JS Vercel Telegram]]
- [[Telegram Web App API передаёт данные пользователя]]
- [[заказ не отправляется в LEADTEX]]
